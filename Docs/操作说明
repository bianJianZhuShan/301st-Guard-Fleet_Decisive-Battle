# 测试操作流程

每次修改 C++ 代码后，按以下步骤测试：

---

## 步骤 1：在 Visual Studio 中编译

1. 打开 `d:\ue\project\newProject\newProject.sln`
2. 确认顶部配置：
   - 左下拉：`Development Editor`
   - 右下拉：`Win64`
3. 在右侧 **解决方案资源管理器** 中：
   - 展开 `Games` 文件夹
   - 找到 `newProject`
   - 右键 → `生成(Build)`
4. 等待输出窗口显示：`1 成功，0 失败`
   - 如果有错误，先修复再继续

---

## 步骤 2：启动 Unreal Editor

1. **关闭**已打开的 Unreal Editor（如果有）
2. 在资源管理器中双击：
   - `d:\ue\project\newProject\newProject.uproject`
3. 等待工程加载完成

---

## 步骤 3：放置测试 Actor

1. 左侧 `放置 Actor` 面板 → 切换到 `所有类(All Classes)`
2. 搜索框输入：`GridSpaceActor`
3. 将其拖到场景视口中
4. 选中后按 `F` 键聚焦到该 Actor

---

## 步骤 4：运行游戏

1. 点击顶部工具栏的 `Play` 按钮（或按 `Alt + P`）
2. 观察效果：
   - 10×10×10 顶点点阵（淡蓝色发光球体）
   - 淡绿色网格线
   - 红/绿/蓝坐标轴
3. 按 `Esc` 退出游戏模式

---

## 步骤 5：调整参数（可选）

在编辑器中选中 `GridSpaceActor`，在 Details 面板可调整：

| 参数 | 说明 |
|------|------|
| `GridSize` | 网格尺寸 (X, Y, Z) |
| `CellSpacing` | 格子间距 |
| `VertexSphereSize` | 顶点球体大小 |
| `bShowVertices` | 显示/隐藏顶点 |
| `bShowGridLines` | 显示/隐藏网格线 |
| `bShowAxisIndicators` | 显示/隐藏坐标轴 |

---

## 步骤 6：保存

1. 菜单 `文件` → `保存当前关卡`
2. 如果是新关卡，起个名字如 `TestMap`

---

## 常见问题

### Q: 搜索不到 GridSpaceActor
- 确认 VS 编译成功（`1 成功，0 失败`）
- 确认重新打开了 UE（不是热重载）

### Q: 看不到网格线
- 网格线只在 `Play` 运行时显示（使用 Debug Draw）
- 确认 `bShowGridLines` 为勾选状态

### Q: 场景太亮/有天空地面
- 在大纲中删除：`SkySphere`、`SkyLight`、`DirectionalLight`、`Floor` 等
- 只保留 `GridSpaceActor` 和 `PlayerStart`

---

## 修改棋子坐标位置及属性

### 修改棋子初始坐标位置

棋子的初始部署位置定义在 `GridSpaceActor.cpp` 的 `SpawnTestUnits()` 函数中：

**文件路径**：`Source/newProject/GridSpaceActor.cpp`

```cpp
void AGridSpaceActor::SpawnTestUnits()
{
    // 己方部署示例
    SpawnUnitAtGridPosition(FIntVector(4, 5, 2), TEXT("护卫舰1"), false);
    // 参数说明：
    // - FIntVector(X, Y, Z)：网格坐标位置
    // - TEXT("单位名称")：单位显示名称
    // - false/true：false=己方，true=敌方

    // 敌方部署示例
    SpawnUnitAtGridPosition(FIntVector(4, 5, 7), TEXT("敌护卫舰1"), true);
}
```

**当前部署模板**：
- 己方：X=0~2（X=2为前排）
- 敌方：X=7~9（X=7为前排）

### 修改棋子属性（统一默认值）

单位的默认属性定义在 `UnitActor.cpp` 的构造函数中：

**文件路径**：`Source/newProject/UnitActor.cpp`

```cpp
AUnitActor::AUnitActor()
{
    // 战斗属性
    Health = 10;        // 当前血量
    MaxHealth = 10;     // 最大血量
    AttackRange = 8;    // 攻击范围（格子数）
    AttackDamage = 5;   // 攻击伤害
    AttackAPCost = 2;   // 攻击消耗AP
    MoveRange = 3;      // 移动范围（格子数）
}
```

### 使用 DataAsset 配置不同单位类型（推荐）

如需为不同舰船类型配置不同属性，可使用 `UnitDataAsset`：

1. 在编辑器中创建：`Content` 右键 → `Miscellaneous` → `Data Asset` → 选择 `UnitDataAsset`
2. 配置属性后，在代码中使用 `SpawnUnitFromDataAsset()` 生成

**文件路径**：`Source/newProject/UnitDataAsset.h`（查看可配置属性）

---

## 旋转功能操作说明

1. 左键选中己方单位
2. 点击底部操作栏的“旋转”按钮进入旋转模式
3. 旋转模式下会高亮半径=3格内的点位
4. 点击高亮点位：单位朝向该点位（支持三维朝向）
5. 点击敌方单位：可直接朝向敌方单位（无需点位高亮）