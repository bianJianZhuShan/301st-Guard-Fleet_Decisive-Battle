# 太空棋类游戏 - 项目说明

## 项目概述
3D太空战术棋类游戏，融合象棋、军棋的策略性和太空战斗的科幻感。
- 引擎: Unreal Engine 5.7
- 语言: C++ + Blueprint

## 开发进度

### v0.0.1 - 核心原型 (开发中)

#### 已完成
- [x] 项目基础框架搭建
- [x] 空间系统 (GridSpaceActor)
  - 10×10×10立方体网格 (1000个顶点)
  - 顶点可视化 (淡蓝色发光球体)
  - 网格线显示 (淡绿色)
  - 坐标轴标识 (X红/Y绿/Z蓝)
  - 格子间距：1000 UE单位
- [x] 单位系统 (UnitActor)
  - 友方单位：蓝灰色长立方体
  - 敌方单位：深灰色长立方体（不可选中/不可操作，可被攻击）
  - 只能位于网格顶点
  - 自动生成测试单位：3友方 + 3敌方
- [x] 移动系统
  - 点击友方单位选中，显示移动范围（球形范围高亮）
  - 鼠标悬停显示移动预览（虚线框、路径线、距离/AP信息）
  - 点击高亮顶点移动单位
  - 单位只能移动到未被占用的顶点
- [x] 战斗系统（基础）
  - 友方单位属性：10 HP、8 Range、5 Damage、2 AP/Attack
  - 攻击按钮进入攻击模式并显示攻击范围
  - 点击目标位置或敌方单位发射投射物（小立方体，水平飞行）
  - 命中扣血，血量归零触发死亡残骸（分裂为 2 个小立方体）
- [x] 战术控制器 (TacticalPlayerController)
  - 鼠标左键选中/移动
  - 鼠标右键取消选择
- [x] 相机控制（已实现核心）
  - 双击友方单位聚焦
  - 滚轮缩放
  - Ctrl/Space 垂直移动
- [x] 战术HUD (TacticalHUD)
  - 右下角显示选中单位信息
  - 显示移动/攻击/旋转按钮和状态
  - 显示血量与攻击属性
  - 底部UI高度为屏幕高度的20%
- [x] 旋转系统
  - 旋转按钮进入旋转模式
  - 旋转模式下高亮半径=3格的点位
  - 悬停显示旋转预览（黄色路径线）
  - 点击空地必须点击高亮点位才生效
  - 点击敌方单位可直接转向
  - 三维球面旋转（支持俯仰）
  - 攻击时自动朝向目标
- [x] 初始部署与舰船表现
  - 敌我部署分界以X轴为准：己方X=0~2，敌方X=7~9
  - 舰船缩放按类型分级：以重巡为基准，每降一级缩小25%
  - 敌方船默认朝向旋转180度面向己方

#### 待实现
- [ ] 回合系统 / 行动点系统（真正扣除与回合切换）
- [ ] 视线检测（障碍物遮挡攻击）
- [ ] 自动反击（平衡核心）
- [ ] 移动动画

---

## 源码结构 (Source/newProject/)

| 文件 | 说明 |
|------|------|
| `newProject.h/cpp` | 模块入口 |
| `newProject.Build.cs` | 构建配置 |
| `GridSpaceActor.h/cpp` | **空间系统** - 10×10×10网格管理器 |
| `UnitActor.h/cpp` | **单位系统** - 可移动的立方体单位 |
| `TacticalPlayerController.h/cpp` | **战术控制器** - 鼠标输入处理 |
| `TacticalGameMode.h/cpp` | **游戏模式** - 设置默认控制器和HUD |
| `TacticalHUD.h/cpp` | **战术HUD** - 单位信息和操作按钮 |

### GridSpaceActor 使用说明

**配置参数:**
- `GridSize`: 网格尺寸，默认 (10, 10, 10)
- `CellSpacing`: 格子间距，默认 1000
- `VertexSphereSize`: 顶点球体大小，默认 0.05

**可视化开关:**
- `bShowVertices`: 显示顶点
- `bShowGridLines`: 显示网格线
- `bShowAxisIndicators`: 显示坐标轴

**主要方法:**
- `GridToWorldPosition()`: 网格坐标 → 世界坐标
- `WorldToGridPosition()`: 世界坐标 → 网格坐标
- `GetVerticesInSphereRange()`: 获取球形范围内的顶点
- `SetVertexOccupied()`: 设置顶点占用状态

---

## 目录结构

| 目录 | 说明 |
|------|------|
| `Source/` | C++源码 |
| `Content/` | 游戏资源 (模型、材质、蓝图等) |
| `Config/` | 项目配置 (.ini) |
| `Docs/` | 设计文档 |
| `Binaries/` | 编译输出 (自动生成) |
| `Intermediate/` | 中间文件 (可删除) |
| `DerivedDataCache/` | 缓存 (可删除) |
| `Saved/` | 日志和存档 |

---

## 快速开始

1. 双击 `newProject.uproject` 打开编辑器
2. 编译C++代码 (Ctrl+Shift+B 或点击Compile)
3. 将 `GridSpaceActor` 拖入场景
4. 运行游戏查看网格效果

## 操作要点

- **选中**
  - 左键点击友方舰船选中
  - 右键取消选择
- **移动**
  - 点击“移动”进入移动模式
  - 悬停查看预览，点击高亮顶点执行移动
- **攻击**
  - 点击“攻击”进入攻击模式
  - 点击敌方舰船或范围内顶点发射投射物
- **旋转**
  - 点击“旋转”进入旋转模式
  - 在半径=3格范围内点击高亮点位，单位朝向该点位
  - 点击敌方单位可直接朝向该单位

## 已知问题

- 可以攻击自己的舰船（当前先不修复）
- 环境光需要在关卡中手动设置（map02 目前光照偏暗）

---

Docs 目录索引请参考: `.keep` 文件（并查看 `开发日志_20241215.md`）